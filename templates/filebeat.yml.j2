{{ ansible_managed | comment('plain', prefix='#################################', postfix='#################################') }}
---
## Log prospectors
filebeat.prospectors:
  {{ filebeat_prospectors | to_yaml | indent(2) }}


## Output configuration
output:
  {{ filebeat_output | to_yaml | indent(2) }}


## General configuration
{{ filebeat_general_config | to_yaml | indent(2) }}
